# Enable workspaces
workspaces=true

# Hoist dependencies to root node_modules when possible
hoist=true

# Save exact versions
save-exact=true

# Strict peer dependencies
strict-peer-dependencies=false

# Use legacy peer deps for Electron compatibility
legacy-peer-deps=false

# Engine strict - disabled for Node 23 compatibility
engine-strict=false
